再帰下降構文解析
- 左結合の演算子をパーズする関数は、パターンとして次のように書きます。
+ 左結合の演算子をパースする関数は、パターンとして次のように書きます。


ステップ5：四則演算のできる言語の作成

- token = tokenize(user_input);
+ token = tokenize();

> ステップ3：トークナイザを導入
ここで定義したerror関数内でexitしているので
以下はいらない
- return 1;